
// 存储各个模块的尺寸信息

DEFAULT_HEIGHT = 5; // 默认高度

ESP32C3_SUPERMINI = [22.76, 18.17, DEFAULT_HEIGHT]; // ESP32C3 SuperMini 模块尺寸（长、宽、高）
MOSFET = [32.8, 16.6, DEFAULT_HEIGHT];              // MOS 管 模块尺寸（长、宽、高）

TERMINAL_BLOCK_A = [31, 12, DEFAULT_HEIGHT];                        // 接线端子
TERMINAL_BLOCK_B = [22, 8.15, DEFAULT_HEIGHT];                      // 接线端子, 五个口，不指定输入正负引脚
TERMINAL_BLOCK_B = [25, 8.15, DEFAULT_HEIGHT];                      // 接线端子, 六个口，不指定输入正负引脚


// 将每一个模块的形状都画出来



